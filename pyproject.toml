[build-system]
requires = ["setuptools"]

[project]
name = "live-game-gen"
version = "0.0.1"
requires-python = "==3.10"
readme = "README.md"
license = {file = "LICENSE"}
dependencies = [
  "pygame>=2.6.0"
]




